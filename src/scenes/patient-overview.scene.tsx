import * as React from 'react';
import { SingleView } from 'src/layouts';
import { Overview } from 'src/pods/my-health/overview.component';

export const PatientOverviewScene = () => (
  <SingleView>
    <Overview />
  </SingleView>
);
