import * as React from 'react';
import { SingleView } from 'src/layouts';
import { Dashboard } from 'src/pods/dashboard/dashboard.component';

export const DashboardScene = () => (
  <SingleView>
    <Dashboard />
  </SingleView>
);
